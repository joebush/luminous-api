type: object
required:
  - packages
properties:
  packages:
    type: array
    minItems: 1
    items:
      $ref: './sales-order-shipment-package-create.yaml'
    description: Array of packages to create for the shipment 